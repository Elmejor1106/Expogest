# Configuración de producción para Render
spring.application.name=expogest

# Puerto dinámico de Render
server.port=${PORT:8080}

# MongoDB URI desde variable de entorno
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://juandavidduranmalaver_db_user:Uts2025.@serverjuan.mdl6ggs.mongodb.net/expogest}

# Auto-crear índices en MongoDB
spring.data.mongodb.auto-index-creation=true

# Configuración de logging
logging.level.root=INFO
logging.level.com.expogest=INFO

# Desactivar DevTools en producción
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false

# Configuración de seguridad
server.error.include-message=never
server.error.include-binding-errors=never
server.error.include-stacktrace=never
server.error.include-exception=false
